# compiler
## compile

### Compile with Cmake:

```bash
mkdir -p build && cd build
cmake .. && make
```

### Compile with makefile: 
In the src directory :

```bash
make
```

## Usage
In the src directory :

```bash
./minigcc <file.c> [output]
```